# SportsStack — Session Wrap-up Tuesday 15 April 2026

## What we completed tonight

### 🗂️ Fixture Import Template — cleaned up
- Removed notes row and sample row — template now outputs headers only (row 1)
- `pitch` column marked mandatory (`pitch *`)
- `round_name` labelled optional (`round_name (optional)`)
- Both column names now also accepted by the importer parser (Bug fix)
- Header detection fixed — `?.v != null` prevents crash on openpyxl-generated files
- File clear button (X) added after upload so users can reset without refreshing
- Bye rows now show `—` in Umpire columns in the preview table
- Allowed Values sheet restructured: Venue–Pitch, Grade, Teams, Clubs (for umpires)
- Venues now filtered by selected association (was previously showing all venues globally)
- Teams deduplicated with `.trim()` on the Set

### 🏟️ Venues
- Added Aerodrome Ovals (Mildura) as Sunraysia Hockey Association's venue
- Added Pitch 1 under that venue

### 📋 Fixture Import — validator improvements
- Added `pitches` table to reference data load
- Pitch/venue conflict validation — throws error if pitch doesn't belong to the selected venue
- Venue filtered by `association_id` in the template download

### 🗓️ Fixtures Management page — major overhaul
- **Fixed fixtures not loading** — page now queries by association when no team is selected
- **Association scope persists** across page navigation via `localStorage` (TeamContext updated)
- **Cascade bar no longer redirects** to Associations page on selection (AppLayout fix)
- **Add Fixture dialog** fully rebuilt:
  - Team dropdown (deduplicated, association-scoped)
  - Opponent dropdown (deduplicated, BYE option)
  - Venue → Pitch chained selectors
  - Umpire 1 & 2 club dropdowns (hidden when BYE selected)
  - Duplicate round + team warning before saving
  - All dropdowns have "— None —" clear option (using `__none__` value to avoid shadcn crash)
- **Vite HMR crash** partially addressed — `hmr.overlay: false` and `watch.usePolling: false` added to vite.config.ts

### 🧪 Test import
- Built and imported `sunraysia_test_fixture_v2.xlsx` — 2 rounds, 4 grades, 8 games
- All 8 rows imported successfully, confirmed in Fixtures Management page

---

## Picking up tomorrow

1. **Team filter on Fixtures Management page** — add a dropdown to filter by team (similar to Status and Round filters already there)
2. **H/A column** — leave as-is; home team is always listed first so it's self-explanatory
3. **Duplicate team names** — parked issue logged in `known-issues.md`. Decision needed on approach before the import is used with real HB data
4. **WHA cleanup** — remove Under 11/12/14 divisions, add venues
5. **Test import with real HB Excel Master Fixture sheet**
6. Consider: fixture export feature, seasons/ladder, player compliance

---

## Key things to remember

- `shadcn <SelectItem>` crashes if `value=""` — always use a sentinel like `"__none__"` instead
- Vite dev server still needs manual `npm run dev` restart after some Antigravity saves despite the config fix
- The cascade bar association picker now uses `localStorage` — first selection in a new session sets it, then it persists automatically
- `known-issues.md` has the duplicate team name problem documented with 3 resolution options
